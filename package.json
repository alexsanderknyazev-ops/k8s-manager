{
  "name": "k8s-manager",
  "private": true,
  "description": "Frontend unit tests use Node built-in test runner (no npm deps).",
  "scripts": {
    "test:js": "node --test static/js/utils.test.mjs"
  }
}
